<p>Listado</p>
<%= link_to 'Nueva foto', :action => 'foto_nueva' %>
<ul>
	<% for foto in @fotos %>	
		<li>
			<h3><%= foto.titulo %></h3>
			<%= image_tag foto.imagen_url.to_s %>
			<p><%= foto.descripcion %></p> 	
		</li>
	<% end %>
</ul>